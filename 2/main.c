#include <stdio.h>
#include <stdlib.h>
#include <math.h>
void main()
{
   float a,b,c;
   printf("enter a number");
   scanf("%f",&a);
   b=sqrt(a);
   c=pow(a,3);
   printf("the number is %f",a);
   printf("the required squarerrot is%f",b);
   printf("the required power is%f",c);
}
